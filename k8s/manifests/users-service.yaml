apiVersion: apps/v1
kind: Deployment
metadata:
  name: nochat-users
  namespace: nochat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nochat-users
  template:
    metadata:
      labels:
        app: nochat-users
    spec:
      containers:
      - name: users
        image: 500532294210.dkr.ecr.us-east-1.amazonaws.com/nochat-users:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
        env:
        - name: DB_HOST
          value: postgres
        - name: DB_NAME
          value: nochat
        - name: DB_USER
          value: postgres
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: password
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: nochat-users
  namespace: nochat
spec:
  ports:
  - port: 8083
    targetPort: 8083
  selector:
    app: nochat-users 