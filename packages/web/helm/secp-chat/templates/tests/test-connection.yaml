apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secp-chat.fullname" . }}-test-connection"
  labels:
    {{- include "secp-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secp-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
