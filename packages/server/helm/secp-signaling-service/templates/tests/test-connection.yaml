apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secp-signaling-service.fullname" . }}-test-connection"
  labels:
    {{- include "secp-signaling-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secp-signaling-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
