apiVersion: v1
kind: Service
metadata:
  name: {{ include "secp-signaling-service.fullname" . }}
  labels:
    {{- include "secp-signaling-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secp-signaling-service.selectorLabels" . | nindent 4 }}
