Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To get the service URL, run:
  kubectl get svc -n {{ .Release.Namespace }} {{ include "secp-signaling-service.fullname" . }}

To check the pods, run:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "secp-signaling-service.name" . }}"
